<Window x:Class="SIMA.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SIMA"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <DockPanel LastChildFill="False" >

        <Border DockPanel.Dock="Top" Background="#2C3E50">
            <DockPanel>
                <Label Foreground="#ffff" >Nombre</Label>
                <TextBox DockPanel.Dock="Left" 
                     Name="txtSearchName" 
                     TextWrapping="Wrap" 
                     Text="Buscar por Nombre " 
                     Width="299" 
                     HorizontalAlignment="Left" 
                     Margin="4,0,0,0"/>

                <Label Foreground="#ffff" > Categoria</Label>
                <ComboBox DockPanel.Dock="Left" 
                         Name="cmbSearchCat"
                         Width="250"
                         HorizontalAlignment="Left"
                         Margin="4,0,0,0"/>

            </DockPanel>
        </Border>
        <Border DockPanel.Dock="Left" Background="#2C3E50" >
        </Border>
        <Border DockPanel.Dock="Right" Width="713">
            <DataGrid  d:ItemsSource="{d:SampleData ItemCount=5}" />
        </Border>

    </DockPanel>

</Window>
